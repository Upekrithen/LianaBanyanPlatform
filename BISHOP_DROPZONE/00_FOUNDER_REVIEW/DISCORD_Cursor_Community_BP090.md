---
target_publication: Discord — Cursor user community
channel: #projects
format: discord-post
anchor: Knight-sessions + cooperative-substrate-as-agent-enhancement + M12
status: DRAFT — FOUNDER DISPATCH AUTHORIZATION REQUIRED
filed: 2026-06-22
nyt_exclusivity_check: CLEAR — no essay prose, no personal narrative, no Alford/sister/chess framing
---

# Channel: #projects

Sharing something that's directly relevant if you use Cursor for long agentic sessions.

The build is Liana Banyan — the agentic layer is called Knight, and it runs in Cursor. Every empirical run, every migration, every benchmark session gets executed by Knight. Last night's Marathon 12 mesh run (`{{ M12_ENSEMBLE }}`% on MMLU-Pro 70Q) was coordinated through a 4-peer Dragon Harness — all orchestrated via Knight sessions.

**What cooperative substrate does for agent sessions:**

The Pheromone substrate (stigmergic index, sub-ms lookup) pre-resolves domain context before the first tool call. What that means in practice: Knight doesn't spend context window re-establishing what it already knows. It starts each session with canonical resolutions injected (Wrasse Scribe pre-injection, 41.1%+ of rote-cognition tokens resolved at session start in Phase E).

The practical effect — same-session compounding. Knight can execute 3+ independent features in a single session because the second task builds directly on the first's already-loaded context. No context degradation, no re-spin overhead.

**This session's architecture** added the Individual Domain Pattern: per-domain model routing with timeout thresholds, plus Star Chamber escalation (4-agent consensus) on low-confidence outputs. The routing logic itself was Knight-built across sequential Marathon sessions.

SSPL + Cooperative Defensive Patent Pledge #2260. $5/yr membership flat. If you're a power Cursor user doing deep agentic builds, happy to compare notes on session architecture.

mnemosynec.org
