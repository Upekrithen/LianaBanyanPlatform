# Knight Marathon Session 23 · M23
## UI Citadel Refactor — Peer/Power Mode · Sidebar Architecture · Quickstart Gate

**Session:** M23 · BP091
**Status:** STAGED FOR FOUNDER RATIFY · R1-R10 gates in Bishop Synthesis
**Authored by:** Sonnet 4.6 SEG (dispatch composition)
**Fires:** After Founder ratifies + after M18b lands + ideally after M22 lands
**Estimated wall-clock:** 12-20 hours (3 blocks + pre-flight + smoke)
**Bishop Synthesis reference:** `C:\Users\Administrator\Documents\LianaBanyanPlatform\BISHOP_DROPZONE\00_FOUNDER_REVIEW\BISHOP_SYNTHESIS_CITADEL_MASTER_PLAN_MNEMOSYNEC_UI_REFACTOR_BP091.md`

---

## Founder Direct Quote

> "We're building a citadel that is planned, not a haphazard city that just grew."
> — Founder, BP091

---

## Pre-Knight Verification Gate — Truth-Always Rook Fallback

**MANDATORY FIRST ACTION BEFORE ANY EDIT.**

Rook's component map (paths, line numbers, component names) was generated by an agent that may not have read the actual files. Bishop has confirmed this caveat. Knight MUST execute the following before Block 1 begins:

### Empirical Component Map Protocol

Knight runs the following in sequence. Results replace Rook's hypothetical map.

**Step 1 — Glob for settings-related components:**
```
Glob: C:\Users\Administrator\Documents\LianaBanyanPlatform\platform\src\**\*Settings*
Glob: C:\Users\Administrator\Documents\LianaBanyanPlatform\platform\src\**\*Tier*
Glob: C:\Users\Administrator\Documents\LianaBanyanPlatform\platform\src\**\*Section_*
```

**Step 2 — Grep for key identifiers Rook mentioned:**
```
Grep pattern: "SettingsContainer" — confirm file exists and locate it
Grep pattern: "AIPowerTier" — confirm component location
Grep pattern: "TierTile" — confirm component location
Grep pattern: "AI That Remembers" — locate the Substrate-Cure violation
Grep pattern: "Cathedral" — locate Caithedral typo (case-insensitive)
Grep pattern: "app:quit" — confirm IPC handler exists or is absent
Grep pattern: "mnemosyne_user_config_v2" — confirm localStorage key
```

**Step 3 — Produce empirical KniPr update before Block 1:**
Knight writes a single KniPr paragraph:

```
KniPr [Pre-Block]: Empirical Component Map verified.
  SettingsContainer: [actual path or NOT FOUND]
  AIPowerTier: [actual path or NOT FOUND]
  TierTile: [actual path or NOT FOUND]
  Section_AICapability: [actual path or NOT FOUND]
  Section_AppVersion: [actual path or NOT FOUND]
  Substrate-Cure violation: [actual file:line or NOT FOUND]
  Caithedral typo: [actual file:line or NOT FOUND]
  app:quit IPC: [EXISTS / ABSENT]
  localStorage key: [CONFIRMED / DIFFERENT KEY]
  Discrepancies from Rook's map: [list or NONE]
```

**If actual structure substantially differs from Rook's claims:** Knight surfaces the discrepancy, adjusts all block targets to match empirical reality, and proceeds. Knight does NOT block on the discrepancy — it adapts.

---

## Block 1 — Phase 1: Foundations (Composes with M18b)

**Goal:** Promote AI Power Tier to main nav · Add Quit · Fix radio bug · Substrate-Cure copy · Caithedral spelling

### T1 — Pre-Block 1 compose check

Knight reads M18b merge status:
```
Bash: git log --oneline -20
Bash: git branch -a | grep m18b
```

If M18b is merged: Block 1 skips the radio fix (§1b) and Caithedral sweep (§1d) — M18b already covered them. Knight notes this in KniPr.

If M18b is NOT merged: Block 1 includes both as fallback items below.

### §1a — Promote AIPowerTier to Main Nav

**Target:** The component currently rendering AI Power Tier content (actual path per Pre-Block empirical map — Knight uses verified path, not Rook's claimed path).

Action:
1. Identify the top-level router or nav component (`App.tsx`, `LayoutMain.tsx`, or equivalent — empirical map confirms).
2. Add a "Models" route/nav item at position 2 (after Home, before Tasks).
3. Move or reference the AIPowerTier component render into the Models route.
4. Verify: AI Power Tier is no longer rendered only inside the Settings scroll. It is a first-class nav destination.
5. Remove AIPowerTier from Settings scroll stack (it should not appear in both places).

**KniPr T1a:** "Models nav item added at position 2. AIPowerTier renders at /models (or equivalent). Removed from Settings scroll. Verified via app launch — Models tab navigates to tier selection without scrolling."

### §1b — Add Quit Button (two locations) — ONLY if M18b did not already add it

**Target:** Top chrome component + sidebar footer component (actual paths per empirical map).

Action:
1. Add Quit button to top chrome — right end, adjacent to mode toggle. Label: "Quit MnemosyneC". Tooltip on hover: "Exits the application. Mesh participation ends."
2. Add Quit button to sidebar footer (Power mode) and page footer (Peer mode). Same label and tooltip.
3. Wire both buttons to the Electron IPC `app:quit` handler. If `app:quit` does not exist in main process (Rook flagged it as MISSING — Knight verifies), add it:
   - File: actual IPC handlers path (empirical map — likely `platform\src\main\ipc\appHandlers.ts` per Rook's hypothesis, Knight confirms)
   - Handler: `ipcMain.handle('app:quit', () => { app.quit(); })`
   - Renderer call: `ipcRenderer.invoke('app:quit')`
4. Verify: Quit button visible in both locations in both Peer and Power modes. Clicking it closes the Electron process (not just the window).

**KniPr T1b:** "Quit button added top chrome + footer. app:quit IPC [added / already existed]. Clicking Quit terminates Electron process confirmed."

### §1c — Substrate-Cure Copy Fix

**Target:** Section_AICapability.tsx (actual path per empirical map — Knight verifies before edit).

Action:
1. Grep for "AI That Remembers" and all case variants.
2. Replace with "The Substrate Cure to AI Amnesia" per canon `canon_tagline_substrate_cure_to_ai_amnesia_supersedes_ai_that_remembers_bp089.eblet.md`.
3. Check entire platform/src tree for additional instances of "AI That Remembers" — sweep is total, not just Section_AICapability.
4. Verify: No instance of "AI That Remembers" remains in rendered UI strings.

**KniPr T1c:** "Substrate-Cure copy fix applied. N instances replaced. Zero remaining 'AI That Remembers' in codebase."

### §1d — Caithedral Spelling Fix — ONLY if M18b §4 sweep did not already resolve

**Target:** Section_AppVersion.tsx (actual path per empirical map — Knight verifies) and any other files per grep.

Action:
1. Grep for "Cathedral" (case-insensitive) across entire platform/src tree.
2. Replace all instances with "Caithedral" where appropriate context confirms it is the cooperative proper noun.
3. Note: if "Cathedral" appears in non-cooperative context (e.g., a comment referencing an external library), leave it.
4. Verify: Grep for "Cathedral" returns zero hits in UI-rendered strings (label props, JSX text, JSON config values).

**KniPr T1d:** "Caithedral sweep complete. N instances corrected. Zero incorrect 'Cathedral' in rendered strings."

### §1e — Hardcoded Model Names Fix

**Target:** TierTile.tsx (actual path per empirical map — Knight verifies).

Action:
1. Identify hardcoded model name strings (Rook flagged `<h3>ULTRA (Gemma 4 12b)</h3>` as example — Knight confirms via grep for hardcoded model names).
2. Extract to a config object or read from existing canon assignment config (grep for existing model assignment config files in platform/src or platform/config).
3. TierTile accepts model name as prop — the parent (AIPowerTier or equivalent) supplies it from config.
4. Verify: Changing the config object updates the displayed model name without touching TierTile.tsx.

**KniPr T1e:** "Hardcoded model names extracted to config. TierTile reads from prop. Verified via config edit."

### §1f — Radio Semantics Fix (fallback — only if M18b not merged)

**Target:** AIPowerTier.tsx or TierTile.tsx (actual path per empirical map — Knight verifies).

Action:
1. Grep for the loose equality comparison Rook flagged (likely `includes` or `==` rather than `===`).
2. Replace with strict equality `===` for active tier comparison.
3. Verify: Only one tier tile shows as active at a time. Multi-active display bug cannot occur.

**KniPr T1f:** "Radio semantics enforced via strict equality. Multi-active bug verified fixed: only one tile active at a time."

### T1 Block Verification Gate

Before proceeding to Block 2, Knight verifies:
- [ ] Models tab appears in main nav at position 2
- [ ] AI Power Tier accessible in ≤ 2 clicks from app launch (no scrolling required)
- [ ] Quit button present in top chrome and footer
- [ ] "Quit MnemosyneC" closes Electron process
- [ ] Zero instances of "AI That Remembers" in codebase
- [ ] Caithedral spelling correct (if M18b not already resolved)
- [ ] Model names dynamic (not hardcoded in TierTile)
- [ ] Radio semantics: only one tier tile active at a time (if M18b not already resolved)

---

## Block 2 — Phase 2: Citadel Gate Architecture

**Goal:** Mode toggle + sidebar navigation + conditional render per mode + localStorage persistence

### §2a — Mode Toggle Component

Create new component: `ModeToggle.tsx`
Location: `platform\src\components\chrome\ModeToggle.tsx` (Knight verifies correct components directory via empirical map)

Spec:
```typescript
// ModeToggle.tsx
// Props: mode: 'peer' | 'power', onChange: (mode: 'peer' | 'power') => void
// Renders: pill toggle, lowercase, top-right position
// Styles: low-key — not a primary CTA. Subtle border, muted color, readable.
// Accessibility: role="switch", aria-checked per mode, keyboard-activatable
```

Behavior:
- "peer" mode renders pill with "peer" label selected
- "power" mode renders pill with "power" label selected
- One click switches. No confirm dialog.
- State persists via localStorage key `mnemosyne_ui_mode` (value: `'peer' | 'power'`)
- Default on first launch: `'peer'`

**KniPr T2a:** "ModeToggle component created. Peer/Power switch renders top-right. localStorage persistence confirmed: mode survives app restart."

### §2b — Sidebar Navigation Component

Create new component: `SidebarNav.tsx`
Location: `platform\src\components\chrome\SidebarNav.tsx`

Nav items:

```
PEER MODE:
  Home
  Models
  Tasks
  Appearance

POWER MODE:
  Home
  Models
  Tasks
  Appearance
  ─────── [visual divider] ───────
  Advanced
  Diagnostics
```

Spec:
- Sidebar visible in Power mode (left rail, fixed width ~220px on desktop)
- Peer mode: sidebar hidden; nav items accessible via simplified top bar (Home, Models, Tasks as tab row)
- Mobile/narrow (< 768px): tab bar layout in both modes (44px touch targets minimum)
- Active item highlighted. Keyboard navigable (arrow keys + enter).
- Footer of sidebar (Power mode): version string + connection indicator + Quit button
- ARIA: `role="navigation"`, `aria-label="Main navigation"`, active item `aria-current="page"`

**KniPr T2b:** "SidebarNav component created. Power mode shows full sidebar with divider. Peer mode shows simplified top bar. Mobile tab bar verified at 360px viewport. ARIA roles confirmed."

### §2c — Conditional Render Integration

**Target:** Top-level layout component (App.tsx or LayoutMain.tsx — empirical map).

Action:
1. Read mode from localStorage on app load. Default to `'peer'` if key absent.
2. Pass mode state down to SidebarNav and top chrome.
3. ModeToggle sits in top chrome and calls the global mode setter.
4. SidebarNav reads mode and renders appropriate items.
5. Content area renders the selected nav item's component.

**KniPr T2c:** "Mode state wired from localStorage → top chrome → SidebarNav → content area. Mode switch live: switching from peer to power instantly reveals Advanced/Diagnostics nav items."

### §2d — localStorage Persistence: Sidebar Collapse State

Action:
1. Add localStorage key `mnemosyne_sidebar_collapsed` (value: `'true' | 'false'`).
2. Power mode sidebar has a collapse toggle (chevron). Collapse state persists across restarts.
3. Peer mode has no collapse toggle (top bar is always the layout).

**KniPr T2d:** "Sidebar collapse state persists. Power → collapse → restart → sidebar remains collapsed. Power → expand → restart → remains expanded."

### T2 Block Verification Gate

Before proceeding to Block 3, Knight verifies:
- [ ] Peer/Power toggle visible top-right in all states
- [ ] Mode switch takes effect immediately (no reload required)
- [ ] Mode persists across app restart
- [ ] Power mode sidebar shows: Home / Models / Tasks / Appearance / divider / Advanced / Diagnostics
- [ ] Peer mode shows simplified top bar (no sidebar)
- [ ] Mobile (360px viewport): tab bar layout, 44px targets confirmed via DevTools
- [ ] Keyboard navigation: arrow keys cycle nav items, Enter activates
- [ ] ARIA: navigation landmark present, active page marked

---

## Block 3 — Phase 3: Quickstart Card + Inner Keep

**Goal:** First-launch portcullis + Advanced section + Diagnostics section + Mesh activity widget

### §3a — Quickstart Card Overlay

Create new component: `QuickstartCard.tsx`
Location: `platform\src\components\onboarding\QuickstartCard.tsx`

Spec:
- Renders as overlay on content area (not full modal — content area dims behind it)
- Shows on first launch (check localStorage key `mnemosyne_quickstart_dismissed`)
- Dismiss button: "Got it — let me in" (single click sets dismissed key, card unmounts)
- Re-summoned via Help menu item "Show Welcome Card" (clears dismissed key, re-renders on next content area paint)
- Does NOT re-appear on subsequent app launches once dismissed
- Does NOT re-appear on login (install-scoped, not identity-scoped)

Card body copy (Founder ratify R8 required for final copy):
```
Welcome to MnemosyneC
The Substrate Cure to AI Amnesia

1. Choose your AI tier under Models
2. Connect to the cooperative mesh
3. You're in.

[Got it — let me in]
```

Mobile: card is full-width, vertically centered, 44px dismiss button.

**KniPr T3a:** "Quickstart Card renders on first launch. Dismiss button works. Card does not re-appear after dismiss on restart. Help → Show Welcome Card re-summons it."

### §3b — Advanced Section Content

**Target:** Advanced nav item → content panel.

Power-user settings surfaces to expose in Advanced:
1. Auto-update toggle (currently in localStorage config — wire to UI)
2. Relay endpoint override (text input — advanced users who run custom relay)
3. Startup behavior (launch at login toggle)
4. Debug logging level (select: off / info / verbose)
5. Reset to defaults button (clears `mnemosyne_user_config_v2`, prompts confirmation)

**Note on hardcoded strings:** all label text uses substrate-neutral vocabulary. No MnemosyneC branding in Advanced section body.

**KniPr T3b:** "Advanced section renders with 5 power-user settings. Reset to defaults prompts confirmation before clearing config. All settings persist to localStorage on change."

### §3c — Diagnostics Section Content

**Target:** Diagnostics nav item (Power mode only) → content panel.

Four surfaces per Bishop Q4 decision:

**Surface 1 — Raw Logs Viewer**
- Read-only
- Filterable by level: All / Info / Warn / Error (segmented control)
- Last 500 lines with vertical scroll
- Auto-refresh every 5 seconds (toggle to pause)
- Clear button (clears display only, not persisted log files)
- IPC: `diagnostic:get-logs` (Knight verifies or creates handler)

**Surface 2 — Active Process List**
- Shows: ollama / relay / inference workers / IPC bridge
- Columns: Name · PID · Uptime · Status (running / idle / error)
- Refresh button
- IPC: `diagnostic:get-processes` (Knight verifies or creates handler)

**Surface 3 — Config JSON Editor**
- Renders current `mnemosyne_user_config_v2` as formatted JSON in a read/write textarea
- "Apply" button saves changes (validates JSON before saving — show parse error inline if invalid)
- "Reset" button reverts textarea to last saved state
- Does NOT auto-save on keystroke (safety — raw JSON editing)

**Surface 4 — Inference Override Sliders**
- Temperature slider (0.0 – 2.0, step 0.1, default 0.7)
- Context window override (512 – 32768, step 512, default from tier config)
- Label: "Power user overrides — resets on app restart unless pinned"
- "Pin these values" toggle (persists to localStorage key `mnemosyne_inference_overrides`)
- IPC: `diagnostic:set-inference-params` (Knight verifies or creates handler)

**KniPr T3c:** "Diagnostics section renders all 4 surfaces. Logs auto-refresh. Process list shows live PIDs. Config editor validates JSON before save. Inference sliders function and pin state persists."

### §3d — Mesh Activity Widget (M22 compose-in)

Knight checks M22 merge status:
```
Bash: git log --oneline -20 | grep -i m22
Bash: git branch -a | grep m22
```

**If M22 is merged:**
- Mesh activity widget imports from M22's data layer (tier-aware routing + MIC rotation data).
- Widget renders in Home nav item content area: peer count / active connections / MIC role indicator / relay status.
- Footer connection indicator reads from same M22 data layer.

**If M22 is NOT yet merged:**
- Stub component renders in Home content area: "Mesh activity coming soon — waiting for M22 to land."
- Footer connection indicator shows static relay ping status (existing behavior, no regression).
- Stub clearly marked with `// M22 compose-in pending` comment in code.

**KniPr T3d:** "Mesh widget [M22 live / M22 stub] in Home content area. Footer connection indicator [M22 data / relay ping]. M22 compose noted in code."

### §3e — NEW Badge for Model Discovery

**Target:** Models nav item + tier tiles within Models content panel.

Action:
1. Add `newSince` timestamp to model config object (set when a new model becomes available).
2. If current date is within 7 days of `newSince`, render a "NEW" badge on the tile.
3. Badge is purely cosmetic — no click required to dismiss. It clears automatically after 7 days.
4. No notification pop-up. No questionnaire. Model appears; badge signals novelty; peer decides.

**KniPr T3e:** "NEW badge appears on model tiles within 7 days of newSince timestamp. Badge auto-clears after 7 days. No pop-up notification."

### T3 Block Verification Gate

Before Block 4, Knight verifies:
- [ ] Quickstart Card shows on first launch
- [ ] Quickstart Card does not reappear after dismiss and restart
- [ ] Help menu re-summons Quickstart Card
- [ ] Advanced section: 5 settings surfaces render and persist
- [ ] Diagnostics section: 4 surfaces render (logs / process list / config editor / sliders)
- [ ] Config JSON editor validates before saving (invalid JSON shows error, does not save)
- [ ] Inference sliders: values persist when pinned
- [ ] Diagnostics section only accessible in Power mode (not visible in Peer mode nav)
- [ ] Mesh activity widget renders (M22 live or stub)
- [ ] NEW badge appears/clears correctly on model tiles

---

## Block 4 — Build, Version Bump, Tower Deploy

### §4a — Version Bump Discussion

Knight and Bishop discuss version number in KniPr before bumping. This is a full architecture change (new sidebar system, mode toggle, Quickstart Card, Diagnostics surfaces). Bishop recommends v0.6.0 (minor version bump — significant architectural change). Founder ratify gate R10 required.

If Founder ratifies v0.6.0: bump `package.json` version to `0.6.0`.
If Founder prefers 0.5.x: Knight uses the next available patch (likely v0.5.19 or Founder directs).

**KniPr T4a:** "Version bumped to [X.Y.Z]. package.json confirmed. Founder ratify R10 [received / pending]."

### §4b — Build

```
Bash: cd C:\Users\Administrator\Documents\LianaBanyanPlatform\platform && npm run build
```

Verify: build completes with zero errors. Warnings documented in KniPr but do not block.

**KniPr T4b:** "Build complete. [N] warnings, 0 errors. Build artifacts at [path]."

### §4c — Electron Smoke Pre-Deploy

Launch Electron app from built artifacts. Verify basic function before Tower deploy:
- App launches to Home screen (not Settings)
- Mode toggle visible top-right
- Models tab navigates to tier selection in ≤ 2 clicks
- Quit button visible and functional
- No console errors on launch

**KniPr T4c:** "Electron smoke pre-deploy passed. [N] console warnings noted. App launches clean."

### §4d — Tower Deploy + Hugo Refresh

```
Bash: [Knight follows existing Tower deploy protocol from prior Marathons]
Bash: [Knight follows existing Hugo refresh protocol]
```

Version string on Tower download page updates to new version. Verify via Tower download page load.

**KniPr T4d:** "Tower deployed. Hugo refreshed. Tower download page shows v[X.Y.Z]. version_trust.json updated (canonical source per BP090 canon)."

---

## Block 5 — Empirical Smoke Tests

### T5a — Founder Phone Walkthrough Test

**Target:** Replicates the exact friction Founder described — walking someone through tier switch by phone.

Protocol:
1. Knight (or Founder on second device) attempts to navigate a fresh peer through tier selection via verbal instruction only.
2. Success criterion: **Target < 60 seconds from app launch to confirmed tier switch, no scrolling required.**
3. Instruction set allowed: "Open the app. Click Models. Select [tier name]. Done."
4. If walkthrough exceeds 60 seconds or requires more than 3 verbal instructions: Knight documents failure mode and proposes targeted fix before shipping.

**KniPr T5a:** "Phone walkthrough test: [PASS / FAIL]. Time to confirmed tier switch: [N] seconds. Instructions required: [N]. Friction points: [none / list]."

### T5b — Mobile / Touchscreen Test

**Target:** DevTools device emulation (iPhone SE / Galaxy S20 viewport).

Protocol:
1. Open app in Electron DevTools device emulation at 375px width.
2. Verify all interactive elements ≥ 44px touch target height.
3. Verify tab bar renders in both Peer and Power modes.
4. Verify Quit button accessible (in footer, not hidden by mobile chrome).
5. Verify Quickstart Card renders correctly on narrow viewport (full-width, centered, dismiss button ≥ 44px).
6. Verify Diagnostics section scrolls correctly on mobile (no horizontal overflow).

**KniPr T5b:** "Mobile test at 375px: [PASS / FAIL]. Touch targets ≥ 44px: [CONFIRMED / VIOLATIONS: list]. Tab bar present: [Y]. Quit accessible: [Y]. No horizontal overflow: [Y]."

### T5c — Accessibility Test

**Target:** Keyboard navigation + ARIA + basic screen reader compatibility.

Protocol:
1. Tab through entire app without mouse. Verify focus order is logical (top-to-bottom, nav before content).
2. Mode toggle: keyboard activatable (Space/Enter). Focus visible.
3. Sidebar nav: arrow keys cycle items. Enter activates.
4. Tier tile selection: keyboard activatable. Active tile has `aria-checked="true"`.
5. Quickstart Card: dismiss button keyboard accessible. Escape key dismisses.
6. Diagnostics surfaces: all form inputs (sliders, textareas, selects) keyboard accessible.
7. ARIA landmarks: `main`, `navigation`, `footer` present.

**KniPr T5c:** "Accessibility test: keyboard nav [PASS / issues: list]. ARIA landmarks [present / missing: list]. Focus visible throughout: [Y/N]. Tier tile aria-checked: [Y]."

### T5d — Regression: Settings Continuity

**Target:** Verify existing settings (theme, tier, autoUpdate) survive the refactor.

Protocol:
1. Set non-default values for theme + tier + autoUpdate before starting Block 1 (document the values).
2. After full M23 completes, open app and verify all three values are the same.
3. Verify localStorage key `mnemosyne_user_config_v2` still contains the same schema (or Knight documents migration if schema changed).

**KniPr T5d:** "Settings continuity: pre-refactor values [theme:X, tier:Y, autoUpdate:Z]. Post-refactor values [same / drifted: list]. localStorage schema [unchanged / migrated to: describe]."

---

## Verification Gates Summary (T1-T20)

| Gate | Block | Criterion |
|---|---|---|
| T1 | Pre-Block | Empirical Component Map produced. Rook discrepancies documented. |
| T2 | 1 | Models tab in main nav at position 2. AIPowerTier accessible ≤ 2 clicks. |
| T3 | 1 | Quit button top chrome + footer. Terminates Electron process. |
| T4 | 1 | Zero "AI That Remembers" instances in codebase. |
| T5 | 1 | Caithedral spelling confirmed correct (if M18b not resolved). |
| T6 | 1 | Model names dynamic (config-driven, not hardcoded). |
| T7 | 1 | Radio semantics: only one tier active at a time (if M18b not resolved). |
| T8 | 2 | Peer/Power toggle top-right, visible always. |
| T9 | 2 | Mode persists across restart. |
| T10 | 2 | Power sidebar: correct 7 items with divider. |
| T11 | 2 | Peer mode: simplified top bar, no sidebar. |
| T12 | 2 | Mobile 360px: tab bar, 44px targets. |
| T13 | 2 | ARIA: navigation landmark, aria-current on active item. |
| T14 | 3 | Quickstart Card: first launch / no re-appear after dismiss / Help re-summons. |
| T15 | 3 | Advanced section: 5 surfaces render and persist. |
| T16 | 3 | Diagnostics: 4 surfaces (logs / processes / config editor / sliders). |
| T17 | 3 | Config editor: validates JSON, does not auto-save. |
| T18 | 3 | Diagnostics inaccessible in Peer mode. |
| T19 | 4 | Build clean. Tower deployed. version_trust.json updated. |
| T20 | 5 | Phone walkthrough < 60s. Mobile test pass. Accessibility keyboard nav pass. |

---

## Out of Scope — Deferred to Future Marathons

The following are noted but explicitly excluded from M23 scope:

- **Run as Service mode** — headless substrate mesh without Electron UI. Future Marathon.
- **Brain Registry UI** — hot-swap cognitive core (v0.6.x CCI per BRAIN-SWAP canon). Future Marathon.
- **Bounty Wall integration in Advanced section** — requires Bounty Wall substrate layer. Future Marathon.
- **Full MIC broadcast signing UI** — I8 security yoke (PENDING per MEMORY.md). Future Marathon.
- **Full screen reader / VoiceOver certification** — M23 does basic ARIA + keyboard. Full audit is a separate accessibility Marathon.
- **Customizable sidebar item order** — user reordering of nav items. Future nice-to-have.
- **Dark / light / system theme implementation** — Appearance nav item is wired but theme engine implementation deferred unless already present.

---

## Ratification Gates (Founder R1-R10)

These gates must be resolved before M23 fires. Full rationale in Bishop Synthesis.

| Gate | Question | Required Before |
|---|---|---|
| R1 | "Peer / Power" as official mode vocabulary? | Block 2 |
| R2 | Unified M23 (not Option B Swift Fix)? | All blocks |
| R3 | Quit = exit Electron process, no background residue? | Block 1 §1b |
| R4 | Diagnostics 4-surface scope confirmed? | Block 3 §3c |
| R5 | "Models" as nav label for AI Power Tier? | Block 1 §1a |
| R6 | M23 fires after M22 lands (not immediately)? | Pre-Knight gate |
| R7 | Mobile MVP in M23 scope (not deferred)? | Block 3 / Block 5 |
| R8 | Quickstart Card copy confirmed? | Block 3 §3a |
| R9 | MnemosyneC name only in title bar + About page? | All blocks |
| R10 | Version bump to v0.6.0 (vs staying on 0.5.x)? | Block 4 §4a |

---

## Estimated Wall-Clock

| Block | Work | Estimated Time |
|---|---|---|
| Pre-Block | Empirical component map | 30-45 min |
| Block 1 | Phase 1: Foundations | 2-4 hrs |
| Block 2 | Phase 2: Citadel Gate Architecture | 4-6 hrs |
| Block 3 | Phase 3: Inner Keep + Quickstart | 3-5 hrs |
| Block 4 | Build + Deploy + Hugo | 1-2 hrs |
| Block 5 | Empirical smoke + phone walkthrough | 1-2 hrs |
| **Total** | | **11.5 – 19.75 hrs** |

---

## Anticipated Return Artifacts

Knight returns to Bishop at M23 close with:

1. **Before/After screenshots** — Settings (old monolithic scroll) vs. Citadel sidebar (new architecture). Both Peer and Power modes captured.
2. **Phone walkthrough receipt** — timestamp of test, time-to-tier-selection metric, instruction count. Pass/fail verdict.
3. **Mobile test screenshot** — 375px viewport showing tab bar layout and 44px touch targets confirmed via DevTools overlay.
4. **Accessibility audit results** — keyboard nav trace (tab order), ARIA landmark inventory, any failures noted.
5. **Version bump receipt** — package.json before/after, version_trust.json updated, Tower download page screenshot.
6. **Empirical Component Map** — the verified component tree that replaced Rook's hypothesis. Permanent artifact for future Marathons.
7. **M22 compose status note** — mesh widget live or stub, with code comment reference.
8. **M18b compose status note** — radio fix and Caithedral sweep absorbed or composed, documented.

---

*Knight Marathon Session 23 · M23 · BP091 · UI Citadel Refactor*
*Dispatch composed by Sonnet 4.6 SEG · 2026-06-22*
*Staged for Founder ratify — do not fire until R1-R10 resolved + M18b landed + M22 ideally landed*
