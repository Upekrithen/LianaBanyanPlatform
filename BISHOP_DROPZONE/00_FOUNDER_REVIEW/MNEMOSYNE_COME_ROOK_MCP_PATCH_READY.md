# MNEMOSYNE COME — Rook MCP Config Patch (ASPIRATIONAL — not active)
**SEG-MC-7 · Wave D · BP079 · 2026-06-10**

## Status: NOT ACTIVE — staged pending Rook billing wall resolution

Rook (Antigravity / Google Gemini) is PAUSED due to Antigravity billing wall.
Per canon: `feedback_rook_paused_billing_wall_bp078.md`

---

## When billing wall resolves:

Configure Rook's MCP client (Antigravity config, if supported) to connect to:

  HTTP+SSE endpoint: `http://[M1-machine-IP]:11482/mcp`
  Auth: Bearer token from `~/.mnemosynec/mcp-secret.txt` on M1

---

## Health check (verify MnemosyneC MCP server is running):

```
GET http://[M1-machine-IP]:11482/mcp/health
Expected: {"status":"ok","version":"0.1.40","port":11482}
```

---

## To enable the HTTP server:

Option 1 — config file:
```
Edit ~/.mnemosynec/config.json → set "remote_mcp_enabled": true
```

Option 2 — manual launch:
```
node librarian-mcp/scripts/mnemosynec-mcp-http.mjs --force
```

---

## Rook MCP client config (when Antigravity supports it)

```json
{
  "mcpServers": {
    "mnemosynec": {
      "transport": "http+sse",
      "url": "http://[M1-machine-IP]:11482/mcp",
      "auth": {
        "type": "bearer",
        "token": "[read from ~/.mnemosynec/mcp-secret.txt]"
      },
      "env": {
        "MNEMOSYNEC_CLIENT_ID": "rook"
      }
    }
  }
}
```

---

## Why HTTP+SSE for Rook (not stdio)

- Rook runs on a separate machine (Antigravity cloud), not the local Windows machine where the MnemosyneC substrate lives
- stdio transport requires a local process — not possible for remote clients
- HTTP+SSE allows Rook to connect from any networked machine to the M1 substrate host
- Knight (Cursor/Windows) and Bishop (Claude Code/Windows) use stdio because they are co-located with the substrate

---

## Status: aspirational — Wave E when Antigravity billing resolves

*Generated by SEG-MC-7 / Knight / BP079 / 2026-06-10*
