# MNEMOSYNE COME — Bishop MCP Config Patch (READY TO APPLY)
**SEG-MC-7 · Wave D · BP079 · 2026-06-10**

## Action: ADD mcpServers block to ~/.claude/settings.json

The current settings.json has NO mcpServers block. Add this at the root level:

```json
{
  "mcpServers": {
    "mnemosynec": {
      "command": "node",
      "args": ["C:\\Users\\Administrator\\Documents\\LianaBanyanPlatform\\librarian-mcp\\scripts\\mnemosynec-mcp-stdio.mjs"],
      "env": {
        "WORKSPACE_ROOT": "C:\\Users\\Administrator\\Documents\\LianaBanyanPlatform",
        "MNEMOSYNEC_CLIENT_ID": "bishop"
      }
    }
  }
}
```

## How to apply

1. Open `C:\Users\Administrator\.claude\settings.json` in a text editor
2. Add the above `mcpServers` block as a top-level key (same level as `"env"`, `"hooks"`, etc.)
3. Save the file
4. Restart Claude Code (Bishop) — the `mcp__mnemosynec__*` tools will appear

## Context: Current Bishop settings.json structure

Bishop's `settings.json` currently contains these top-level keys but NO `mcpServers` block:
- `"env"` — environment variables
- `"extraKnownMarketplaces"` — marketplace config
- `"hooks"` — session hooks
- `"statusLine"` — CLI status line
- `"permissions"` — tool permissions
- `"skipWorkflowUsageWarning"` — boolean flag

Adding `mcpServers` ADDS the MnemosyneC MCP alongside the harness-injected MCPs (librarian, knight-bishop-bridge, etc.) — it does NOT replace them.

## Verification

After restart, call: `mcp__mnemosynec__ping`
Expected response: `{"pong": true, "version": "0.1.39", "shim": "mnemosynec-mcp-stdio"}`

After ping succeeds, confirm round-trip:
1. Bishop calls `mcp__mnemosynec__send_message` to Knight
2. Knight calls `mcp__mnemosynec__check_messages` — message should appear
3. Both sides confirm: Yoke bridge working through MnemosyneC substrate

## Transition Plan

**Wave D (now):** Bishop has BOTH harness-injected `mcp__librarian__*` AND new `mcp__mnemosynec__*`. Use `mcp__mnemosynec__*` for all substrate ops; keep librarian as fallback.

**Wave D+1 (after parity confirmed):** Retire harness-injected `mcp__knight-bishop-bridge__*` and deprecated `mcp__librarian__*`. Requires Founder + Bishop to coordinate harness config update (mechanism TBD — Bishop cannot self-modify harness-injected MCPs).

## Rollback

If MnemosyneC has a fatal bug after applying:
- Remove the `mcpServers` block from `~/.claude/settings.json`
- Restart Claude Code
- Bishop reverts to harness-injected MCPs only
- Disk Yoke fallback (`BISHOP_DROPZONE\01_KnightPrompts\`) remains live regardless

## Status: STAGED — apply after Founder installs MnemosyneC v0.1.40 on M1

*Generated by SEG-MC-7 / Knight / BP079 / 2026-06-10*
