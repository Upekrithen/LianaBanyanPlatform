# MIC · M22 CTA · Block 3 · BP092

**Status:** PASS (conservative minimum)  
**Timestamp:** 2026-06-22T22:27:43

## Change

`platform/src/components/ProtectedRoute.tsx`:

- `ProtectedRoute`: `Navigate to="/auth"` → `Navigate to="/join"`
- `CaptainRoute` (unauthenticated): same change

Blurred-preview + inline `MembershipGateModal` deferred per Bishop conservative recommendation.

## Return-path wiring

- `lb_auth_return_path` set in `ProtectedRoute` before redirect — confirmed.
- Read on auth success in `Auth.tsx` — confirmed.
- **Flag:** post-Stripe `/membership-success` does not read `lb_auth_return_path`; join-complete return may need follow-up dispatch (edge function scope).
