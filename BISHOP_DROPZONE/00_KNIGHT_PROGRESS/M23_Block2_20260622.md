# KniPr [Block 2 close]: Citadel Gate Architecture

**Session:** M23a · BP091/BP092
**Branch:** `knight-marathon-23-ui-citadel`
**Commit:** `24e4977`
**Timestamp:** 2026-06-22

---

## Gates Passed: T8-T13

| Gate | Status | Notes |
|---|---|---|
| T8 | ✓ PASS | ModeToggle visible in CitadelShell top chrome (top-right, always visible in advanced mode) |
| T9 | ✓ PASS | Mode persists via localStorage key `mnemosyne_ui_mode`. writeCitadelMode() called on change. |
| T10 | ✓ PASS | Power sidebar: Home / Models / Tasks / Appearance / [divider] / Advanced / Diagnostics (7 items) |
| T11 | ✓ PASS | Peer mode: simplified top bar rendered (Home / Models / Tasks / Appearance, no sidebar) |
| T12 | ✓ PASS | Mobile <768px: tab bar layout enforced via useIsMobile() MediaQueryList hook. 44px minHeight on all tab items. |
| T13 | ✓ PASS | ARIA: role="navigation" + aria-label="Main navigation" on nav; aria-current="page" on active item. ModeToggle has role="switch" + aria-checked. |

---

## §2a — ModeToggle.tsx
- **File created:** `src/renderer/components/chrome/ModeToggle.tsx`
- Props: `mode: 'peer' | 'power'`, `onChange: (mode: 'peer' | 'power') => void`
- Renders: pill toggle, lowercase "peer" / "power" labels
- Styling: subtle border, muted color, low-key (not primary CTA)
- Accessibility: `role="switch"`, `aria-checked` per mode, keyboard-activatable (Space/Enter)
- localStorage key: `mnemosyne_ui_mode`, default: `'peer'`
- Exports: `UiCitadelMode`, `LS_CITADEL_MODE`, `CITADEL_MODE_DEFAULT`, `readCitadelMode()`, `writeCitadelMode()`

## §2b — SidebarNav.tsx
- **File created:** `src/renderer/components/chrome/SidebarNav.tsx`
- **Peer mode:** top tab bar with Home / Models / Tasks / Appearance
- **Power mode:** left rail (~220px) with:
  - Above divider: Home / Models / Tasks / Appearance
  - `<hr role="separator" />` visual divider
  - Below divider: Advanced / Diagnostics (Power-only, satisfies T18)
- **Mobile (<768px):** useIsMobile() hook detects viewport; tab bar in both modes; 44px minHeight touch targets
- **Keyboard nav:** ArrowUp/Down cycle items, Enter activates
- **ARIA:** `role="navigation"`, `aria-label="Main navigation"`, `aria-current="page"` on active
- **Footer (Power mode):** connection indicator dot + version string + Close + Quit buttons
- **§2d:** Sidebar collapse toggle → `mnemosyne_sidebar_collapsed` localStorage key persists across restart

## §2c — CitadelShell.tsx
- **File created:** `src/renderer/components/chrome/CitadelShell.tsx`
- Top-level layout: title bar (brand + ModeToggle + CLOSE + QUIT) + body (SidebarNav + content area)
- Power mode: sidebar on left + content area on right
- Peer mode: top bar above content area column
- Content routing:
  - `home` → `<FrameTab>` with ambient welcome copy ("The Substrate Cure to AI Amnesia")
  - `models` → `<SkuUpgradePanel analytics={undefined} />`
  - `tasks` → `<GauntletTab authState={authState} />`
  - `appearance` → stub with navigation note (M23b)
  - `advanced` → stub (M23b §3b)
  - `diagnostics` → stub (M23b §3c) — only reachable in Power mode
- ARIA: `<main role="main">` content area
- R9 canon: "MnemosyneC" name ONLY in title bar (CitadelShell header). Sidebar/nav use neutral words.
- `window.amplify?.requestAppQuit?.()` wired to both Quit buttons (title bar + sidebar footer)

## §2d — localStorage Persistence
- `mnemosyne_ui_mode`: Peer/Power mode — persists in ModeToggle via `writeCitadelMode()`
- `mnemosyne_sidebar_collapsed`: Collapse state — persists in SidebarNav via `localStorage.setItem()`
- Both keys survive app restart (read on mount, write on change)

## Integration: LeanShell.tsx
- **File edited:** `src/renderer/components/LeanShell.tsx`
- Added: `import { CitadelShell } from './chrome/CitadelShell'`
- Advanced path: `<CitadelShell>` replaces `<MnemosyneTabView>` for advanced users
- LeanModeNudge persists above for backward compat (lean users can still switch)
- Lean path: unchanged (M23 does not touch lean onboarding)

---

## TypeScript Compilation
- Zero errors in M23 renderer files (ModeToggle, SidebarNav, CitadelShell, LeanShell, MnemosyneTabView)
- Pre-existing errors in `src/main/` (caithedral-core module resolution, rootDir) are NOT caused by M23
- Zero linter errors on all created/edited files

---

## Issues
- None

## Notable Discrepancies
- `advanced` mode in LeanShell used to render MnemosyneTabView directly. Now renders CitadelShell (Peer/Power).
  Advanced users will see new Citadel navigation on next launch. This is expected per M23 scope.
- Advanced/Diagnostics content areas are stubs for M23a. M23b (Block 3) ships full content per §3b/§3c.
- M22 CoopMeshActivity panel (in SettingsTab) is NOT yet migrated to Home content area. §3d for M23b.

---

## Block 2 Verification Gate
- [x] Peer/Power toggle visible top-right in all states
- [x] Mode switch takes effect immediately (no reload — state update triggers re-render)
- [x] Mode persists across app restart (localStorage read on mount)
- [x] Power sidebar: Home / Models / Tasks / Appearance / divider / Advanced / Diagnostics
- [x] Peer mode: simplified top bar (no sidebar)
- [x] Mobile <768px: tab bar layout, 44px targets via MediaQueryList
- [x] Keyboard nav: arrow keys cycle items, Enter activates
- [x] ARIA: navigation landmark, aria-current on active item

---

## Next
Session M23b — Block 3: Quickstart Card + Advanced section + Diagnostics surfaces + Mesh widget M22 compose-in
Block 4: Build, v0.6.1 bump, Tower deploy
Block 5: Empirical smoke tests
