# KniPr [Block 1 close]: Foundations

**Session:** M23a · BP091/BP092
**Branch:** `knight-marathon-23-ui-citadel`
**Commit:** `24e4977` (cherry-picked to correct branch)
**Timestamp:** 2026-06-22

---

## Gates Passed: T2-T7

| Gate | Status | Notes |
|---|---|---|
| T2 | ✓ PASS | Models tab added at position 2 in MnemosyneTabView TABS array. SkuUpgradePanel renders as panel-models. |
| T3 | ✓ PASS | QUIT button added to MnemosyneTabView title bar (calls window.amplify?.requestAppQuit?.()); CLOSE button (×) retained for close-to-background. app:request-quit safeHandle added to index.ts registerIPCHandlers(). |
| T4 | ✓ PASS | Zero instances of "AI That Remembers" remain. WelcomeView.tsx:203 → "The Substrate Cure to AI Amnesia — runs locally, belongs to you." ShareMnemosyneC.tsx:101 → "The Substrate Cure to AI Amnesia — free forever, runs locally, belongs to you." |
| T5 | ✓ PASS | Caithedral spelling verified correct throughout. M18b §4 sweep absorbed. No edit required. |
| T6 | ✓ PASS | SKU_TIERS typed config array already present in SkuUpgradePanel.tsx:41-87. TIER_MODEL_MAP at lines 93-99. M18b already extracted. No edit required. |
| T7 | ✓ PASS | getTileState() already uses strict === at SkuUpgradePanel.tsx:106. M18b already fixed. No edit required. |

---

## §1a — Promote Models to Main Nav
- **File edited:** `src/renderer/components/MnemosyneTabView.tsx`
- Added `'models'` to `TabId` type union
- Added `'models'` to `PRIORITY_TAB_IDS` at position 2 (frame, **models**, ai-selector, ...)
- Added `{ id: 'models', label: 'Models', icon: '🧠', tooltip: '...' }` to `TABS` array at position 2
- Added `import { SkuUpgradePanel } from './SkuUpgradePanel'`
- Added `activeTab === 'models'` panel rendering `<SkuUpgradePanel analytics={undefined} />`
- AI Power Tier accessible in ≤ 2 clicks from launch (click window → click Models tab)
- Settings scroll still contains SkuUpgradePanel (not removed from Settings to avoid regression)

## §1b — CLOSE and QUIT Buttons (R3 RESOLVED: two-button semantic)
- **File edited:** `src/main/index.ts`
  - Added `safeHandle('app:request-quit', () => { app.quit(); })` inside `registerIPCHandlers()`
  - Near existing `ipcMain.on('request-app-quit', ...)` at line ~1640
- **File edited:** `src/renderer/components/MnemosyneTabView.tsx`
  - CLOSE (×) button: retains `onClick={onClose}` (window.close → close to background, mesh alive)
  - QUIT button: calls `window.amplify?.requestAppQuit?.()` → `app:request-quit` → `app.quit()`
  - Tooltip distinction enforces R3 semantic in UX
- **Note:** `requestAppQuit` was ALREADY in preload.ts:1627 — only main handler was missing

## §1c — Substrate-Cure Copy Fix
- **Files edited:** `WelcomeView.tsx:203` + `ShareMnemosyneC.tsx:101`
- `'Free AI that remembers'` → `'The Substrate Cure to AI Amnesia'` (WelcomeView)
- `'Share the AI that remembers'` → `'The Substrate Cure to AI Amnesia'` (ShareMnemosyneC)
- Zero instances of "AI That Remembers" remain in src/ (verified)

## §1d — Caithedral Spelling
- SKIP (M18b absorbed) — verified correct, no edit needed
- `SkuUpgradePanel.tsx:710` shows `"Caithedral AI Tier"` — correct spelling

## §1e — Hardcoded Model Names
- SKIP (M18b absorbed) — SKU_TIERS typed array is the config; TIER_MODEL_MAP at lines 93-99
- No TierTile.tsx exists; names are not hardcoded in render JSX

## §1f — Radio Semantics
- SKIP (M18b absorbed) — getTileState() strict `===` at SkuUpgradePanel.tsx:106

---

## Issues
- None

## Notable Discrepancies
- `app:request-quit` handler was genuinely ABSENT from index.ts (not just named differently). Added as `safeHandle('app:request-quit', () => app.quit())`. Preload bridge was already wired.
- SettingsTab.tsx grew to 3098 lines (from 2633 in empirical map) due to M22 Blk6 Coop Mesh Activity panel

## Next
Block 2 — Citadel Gate Architecture (§2a–§2d)
