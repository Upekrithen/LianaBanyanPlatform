# KNIGHT SESSION 225 — LIBRARIAN INDEX REBUILD + MCP RESTART

## Priority: HIGH (Bishop needs accurate stats for B061 work)
## Estimated time: 5 minutes
## Domain: Infrastructure

---

## CONTEXT

K224 Defense domain migration is COMPLETE (16 files, 4 pages, 6 components, 2 hooks, 2 libs, routes, barrel). But the Librarian indexes haven't been rebuilt since — `get_migration_status` still shows defense as `not_started` with 0 v2 pages/comps. After rebuild, the count should go from **16/23 to 17/23**.

---

## TASK 1 — Rebuild Librarian Indexes

```bash
cd librarian-mcp
npx tsc && node dist/indexer/buildIndex.js
```

Verify output includes:
- v2-migration.json shows **17/23** domains migrated
- Defense domain shows `migrated` with 4 v2 pages, 6 v2 components
- domains.json includes `defense` with all file paths
- overview.json updated counts

## TASK 2 — Restart Librarian MCP Server

The MCP server caches indexes at startup. Even after rebuild, the running process serves stale data. To restart:

1. Open Claude Desktop settings → MCP Servers
2. Find `librarian-mcp` entry
3. Toggle it OFF, wait 3 seconds, toggle it ON
4. OR: Close and reopen Claude Desktop entirely

## TASK 3 — Verify

After restart, run this in the Librarian:
- `get_migration_status('list')` → should show defense = migrated, total = 17/23
- `get_system_overview()` → verify counts are fresh

---

## REMAINING UN-MIGRATED DOMAINS (6 after defense)

| Domain | v1 Tables | v1 Pages | v1 Funcs | Notes |
|--------|-----------|----------|----------|-------|
| admin | 1 | 0 | 3 | Admin tools |
| beacon | 6 | 4 | 1 | Beacons/alerts |
| calendar | 2 | 1 | 3 | Events/scheduling |
| initiatives | 0 | 0 | 0 | May be empty shell |
| political | 1 | 3 | 1 | Political Expedition |
| vehicle | 2 | 3 | 0 | Local Wheels/Lemon Lot |

Gaming is already `migrated` (K223 Session 1 built 6 pages) but Sessions 2-8 remain for full wiring.

---

*Generated by Bishop B061. FOR THE KEEP!*
