# PROMPT_KNIGHT_BUSHEL_7_3_LAYER_TAXONOMY_COVERAGE_AUDIT_BP021

**Bushel 7 — 3-Layer Strategic Taxonomy + Coverage Audit** | BP021 | depth-3 nested architecture (1 Knight × 8 Shadow subagents × 8 sub-subagents = 64 nested cP) | Config B all-Sonnet | one Knight tab | Founder pre-ratified BP020 turns ~115 / 140 / 150 / 155.

---

## Mission

You are the Knight orchestrator firing **Bushel 7 — 3-Layer Strategic Taxonomy + Coverage Audit**. This is the highest-leverage Bushel possible: the strategic-taxonomy-and-coverage exercise that produces all three layer manifests in one fire. It solves productization (Candelabra ships clearly), preserves vision (Cooperative Datacenter roadmap), protects IP (Patent Reserve covers everything), and self-productizes as a shippable LB Frame template.

Audit ~80+ architectural primitives ratified across BP001-BP020. Partition them across 8 primitive-class shards (Shadow subagents). Each Shadow enumerates its shard's primitives from substrate, partitions them across up to 8 sub-subagents, and each sub-subagent performs the 12-field per-primitive deep audit. Results aggregate to 3 layer manifests + gap-list + roadmap + filing prioritization + completeness scorecard.

**BP020 prior-Bushel context**: Bushel 1 Reckoning fired 8 cP / 1,514 entries / 29 min / ~$12 (Codex `LB-CODEX-0023`). Bushel 2 Pixie-Dust fired 1+8+64 cP three-tier scaling showcase / 4,900+ entries (Codex `LB-CODEX-0024`). Bushel 7 fires at 64 cP all-Sonnet — the Tier 3 architecture validated by Bushel 2's scaling-curve receipt + BP020 depth-3 nested-subagent probe PASS (`probe-depth-3-nested-subagent-bp020-pass`, salt `6b6a59`, ts 2026-05-03T13:14:30Z, commit `c1b6aa2`).

**Depth-3 architecture LOCKED post-probe**: Cursor agent-mode supports nested subagent spawning at depth-3. This Knight tab can orchestrate 8 Shadows × 8 sub-subagents = 64 nested cylinders without multi-tab fallback.

---

## WRASSE PRE-INJECTION (per `feedback_kprompt_wrasse_prelude_convention.md` B133)

Trigger keywords for substrate routing:
- bushel 7 3 layer strategic taxonomy coverage audit
- candelabra core cooperative datacenter dream patent reserve
- depth-3 nested subagent 64 candlepower bp021
- per-primitive layer assignment pheromone aa cephas code mcp hook runtime
- prov 16 candidate competitive moat consumer facing maintenance burden
- 8 primitive class shards substrate scribe detective apiarist currency librarian defense discipline innovation
- shippable lb frame product template diagnostic organization prioritization bushel
- titan within titan within titan all sonnet config b
- aggregate completeness scorecard gap-list filing prioritization

---

## Phase A — Brief and Plan

1. Call `mcp__librarian__brief_me` with task: `"Bushel 7 3-Layer Strategic Taxonomy and Coverage Audit Knight orchestrator BP021 depth-3 64cP"`
2. Read these canon files for full scope context:
   - `~/.claude/state/bishop_coffee.md` — BP020→BP021 handoff with Bushel 7 design + 3-layer architecture
   - `~/.claude/state/eblets/CANON/titan_within_titan_subagent_fanout_k_prompt_template_canon_bp020.eblet.md` — fan-out canon
   - `~/.claude/state/eblets/CANON/substrate_as_immutable_backup_pyramid_indexed_canon_bp020.eblet.md` — substrate-as-backup property (live-tested 2× in BP020)
   - `~/.claude/state/eblets/CANON/candlepower_cp_lb_throughput_unit_canon_bp020.eblet.md` — cP unit
   - `~/.claude/state/eblets/CANON/fire_code_universal_publication_gate_bishop_discipline_bp020.eblet.md` — Founder Fire Code is universal gate
   - `~/.claude/state/probe/depth_3_validation/probe_result.jsonl` — depth-3 PASS receipt
3. Verify substrate workspace exists: create `~/.claude/state/bushel_7/` if not present
4. Update todo list (use TodoWrite) with Phase A done, Phase B-F queued

## Phase B — 8-Shadow Shard Spec

Spawn 8 Cursor agent-mode Shadow subagents in parallel. Each Shadow owns one primitive-class shard:

```
Shadow 1 — Substrate primitives
  Scope: Wrasse Eblet routing / Pheromone substrate / Detective dispatch / Augur living gate /
         Substrate-as-immutable-backup / Multi-trail pheromone flavor-class / Bags of Holding /
         Substrate-routed memory expansion / Ring of Three Golden Eblets / Federation organism
  Substrate paths: ~/.claude/state/eblets/CANON/, ~/.claude/state/wrasse/, ~/.claude/state/pheromone/

Shadow 2 — Scribe primitives
  Scope: Catechist Scribe / Augur Living Gate / The Shadow / Reminder Scribe / Tidbit Scribe /
         House Scribe / Legacy Scribe / Bureau Scribe / Cathedral Scribes / Excalibur Class Scribes
  Substrate paths: ~/.claude/state/eblets/CANON/*scribe*, BISHOP_DROPZONE/12_Innovations_AA/*SCRIBE*

Shadow 3 — Detective + Apiarist primitives
  Scope: Detective TEAM substrate writeback / Pheromone deep-extraction / Apiarist Hive /
         Bloodhound rebuild / Cross-cathedral provenance / Hive thread federation /
         House Scribe coordinate translation / Population audits
  Substrate paths: ~/.claude/state/eblets/CANON/*detective*, *apiarist*, *hive*

Shadow 4 — Currency primitives
  Scope: Three currencies (Credits / Marks / Joules) / Mark-backing one-way valve /
         Joules audit + balance + mint + redeem + transfer / Joules forever-stamp /
         Cost+20% platform margin / 83.3% creator-keeps / Repeat-Player ratio /
         Slow Blade defense stack / Furnace
  Substrate paths: BISHOP_DROPZONE/12_Innovations_AA/*JOULE*, *MARK*, *CURRENCY*,
                   ~/.claude/state/eblets/CANON/*currency*, *joules*, *marks*

Shadow 5 — Librarian product primitives
  Scope: LB Librarian "unparalleled accuracy speed diminished cost" / "It's HOW You Use It" /
         librarian.lianabanyan.com surface / Brittle vs Fluid Librarian by cohort-class /
         Cohort class enforcement / Cue Card 7-day vesting / brief_me / Federation vs
         Non-Federation Scribe sharing / Apiarist Librarian Hive / Atreyu / Thirteenth Warrior
  Substrate paths: ~/.claude/state/eblets/CANON/*librarian*, *cohort_class*, *cue_card*,
                   platform/src/components/librarian/, librarian-mcp/

Shadow 6 — Defense primitives
  Scope: Slow Blade V2 8-vector / Furnace + Six Sparks + Trust Match + Seasoning + Good
         Standing Roll / Glass Door / Cooperative Defensive Patent Pledge (#2260) /
         Slow Blade architecture / Adversarial fence testing / Algorithmic Efficiency Mandate
  Substrate paths: BISHOP_DROPZONE/12_Innovations_AA/*SLOW_BLADE*, *FURNACE*, *DEFENSE*,
                   ~/.claude/state/eblets/CANON/*slow_blade*, *defense*, *adversarial*

Shadow 7 — Discipline primitives
  Scope: Bishop's Coffee + Breakfast hand-crank / Catechist R01-R10 / Augur living gate /
         Wrasse pre-injection convention / Knight K-prompt format / Brick Wall write-without-
         asking / Advance permissions / Fire Code universal publication gate / Krisscross
         reciprocal reboot / Coal-Shovel-Tag / Slow is smooth smooth is fast
  Substrate paths: ~/.claude/state/eblets/CANON/*coffee*, *breakfast*, *catechist*, *fire_code*,
                   ~/.claude/projects/.../memory/feedback_*.md

Shadow 8 — Innovation/Patent primitives
  Scope: 15 provisionals filed (Prov 13/14/15 + history) / Prov 16 in flight / 228 Crown
         Jewels / 2,506 formal claims / 2,270 innovations / IP load-balancing 60/20/10/10 /
         A&A formal pipeline / Bedrock Foundation Chandelier / R&D Battery /
         Pheromone-anchored decisions / Social-DAG canon
  Substrate paths: BISHOP_DROPZONE/12_Innovations_AA/, BISHOP_DROPZONE/03_BishopHandoffs/PROV_*,
                   ~/.claude/state/eblets/CANON/*prov*, *innovation*, *crown_jewel*
```

For each Shadow, provide the per-primitive audit schema (Phase C below) + sub-subagent template + per-process salt directive + append-only target path.

## Phase C — Per-Primitive Audit Schema (12 fields)

Each per-primitive audit JSONL line MUST populate all 12 fields:

```jsonl
{
  "primitive_id": "<slug>",
  "primitive_name": "<canonical name>",
  "shard": "<1-8>",
  "shard_class": "substrate|scribe|detective_apiarist|currency|librarian|defense|discipline|innovation_patent",
  "ratification_session": "<BP-NNN or B-NNN>",
  "audit": {
    "layer_assignment": "candelabra_core|cooperative_datacenter_dream|patent_reserve",
    "pheromone_write_status": "written|unwritten|partial",
    "aa_formal_status": "filed|drafted|scaffolded|none",
    "cephas_page_status": "live|drafted|not_started",
    "code_implementation_status": "shipped|partial|not_started|in_review",
    "mcp_tool_registered": true|false,
    "hook_wired": true|false,
    "runtime_tested": true|false,
    "prov_16_candidate": "yes|no|future_prov",
    "competitive_moat_strength": "high|medium|low",
    "consumer_facing_value": "high|medium|low|internal_only",
    "maintenance_burden_estimate": "low|medium|high"
  },
  "evidence": {
    "eblet_path": "<path or null>",
    "aa_formal_path": "<path or null>",
    "cephas_page_path": "<path or null>",
    "code_paths": ["<path>", "..."],
    "mcp_tool_name": "<name or null>",
    "hook_path": "<path or null>",
    "test_receipt_path": "<path or null>"
  },
  "rationale": {
    "layer_choice": "<one sentence>",
    "moat_basis": "<one sentence>",
    "consumer_value_basis": "<one sentence>"
  },
  "ts": "<ISO-8601>",
  "hmac": "<...>",
  "chronos": "<...>",
  "salt": "<3-byte hex per-process>"
}
```

**Layer-assignment heuristics** (sub-subagents apply these consistently):
- **candelabra_core**: load-bearing for day-1 ship product (substrate-routed memory, multi-cylinder firing, Codex Layer 8, Stats-Capture, Detective TEAM, Substrate-as-immutable-backup, Fire Code discipline, brief_me, the 7 stanchions of LB Librarian product)
- **cooperative_datacenter_dream**: BUILD-FOR-REAL composes-with-Candelabra; Atreyu, Apiarist, Thirteenth Warrior, Excalibur Class, Federation organism, Sphinx Federation, Conductor's Baton, Krisscross, Multi-trail pheromone flavor-class
- **patent_reserve**: file-first build-when-needed under #2260 Cooperative Defensive Patent Pledge umbrella — everything else with patent-defensible competitive moat

## Phase D — Sub-Subagent Template (paste into each sub-subagent that each Shadow spawns)

```
You are sub-subagent {{S}}/8 for Shadow {{N}} ({{SHARD_CLASS}}) Bushel 7 fire BP021.

Your assignment: {{PRIMITIVE_RANGE}} (~1-3 primitives from this shard's enumeration).

For each primitive, perform the 12-field audit per the schema in the parent Shadow brief.

Method:
1. Read primary substrate evidence: Eblet (if any) + A&A formal (if any) + Cephas page (if any) + code paths (Glob + Read for impl files).
2. Determine layer_assignment using the Phase C heuristics — choose the layer where the primitive does the most work, not the most layers it touches.
3. Verify each status field by reading the actual artifact (status comes from disk, not memory). pheromone_write_status: check ~/.claude/state/pheromone/ scribes. aa_formal_status: check BISHOP_DROPZONE/12_Innovations_AA/. cephas_page_status: check platform/src/pages/cephas/ + content/. code_implementation_status: check platform/src/ + librarian-mcp/. mcp_tool_registered: check librarian-mcp/server.ts MCP tool exports. hook_wired: check ~/.claude/settings.json + ~/.claude/projects/.../settings.json. runtime_tested: check for test_receipt or empirical evidence path.
4. Score competitive_moat_strength + consumer_facing_value + maintenance_burden_estimate using the rationale fields — write one-sentence justifications grounded in evidence, not vibes.
5. Append each primitive's audit entry as one JSONL line to ~/.claude/state/bushel_7/shard_{{N}}_{{SHARD_SLUG}}.jsonl. Use per-process salt randomBytes(3).toString("hex") in HMAC + IDs to prevent collision with peer sub-subagents writing concurrently per BP018 KN-D2 fix.

Per BP020 substrate-as-immutable-backup canon, your work is preserved on disk regardless of any sub-subagent state-loss. Append-only JSONL semantics handle concurrent writes.

When your assignment is complete, report back to parent Shadow with: count of entries written, gap-list (primitives where evidence couldn't be located), any cleaner-than-found findings.

DO NOT attempt repairs. DO NOT guess at status values. DO NOT skip primitives — flag with reason instead.
```

## Phase E — Shadow Aggregation (each Shadow does this after its 8 sub-subagents return)

Each Shadow:
1. Verifies its shard JSONL completeness (target ≥95% of enumerated primitives audited)
2. Computes shard summary: primitive count / per-layer count / status field histogram / gap count
3. Appends shard summary record to its shard JSONL with `record_class: "shard_summary"`
4. Reports back to parent Knight with substrate path + counts + gap-list

## Phase F — Knight Aggregation + 3 Layer Manifests + Scorecard + Codex Bind

After all 8 Shadows return:

1. Read all 8 shard JSONLs. Combined entry count target ≥75 (assuming ~80+ primitives × 95% completion).
2. Partition primitives by `audit.layer_assignment` into 3 sets.
3. Write 3 layer manifests:
   - `~/.claude/state/bushel_7/manifest_layer_1_candelabra_core.json` — Candelabra Core spec (the Product, day-1 ship). Per-primitive: shard / status fields / code paths / gap-list (what's needed to ship).
   - `~/.claude/state/bushel_7/manifest_layer_2_cooperative_datacenter_dream.json` — Cooperative Datacenter implementation roadmap. Per-primitive: build-order suggestion (Atreyu → Apiarist → Thirteenth Warrior is Founder-stated baseline) + composition-with-Candelabra notes.
   - `~/.claude/state/bushel_7/manifest_layer_3_patent_reserve.json` — Patent Reserve filing-priority list. Per-primitive: prov_16_candidate flag + competitive_moat_strength + filing-when recommendation under #2260 umbrella.
4. Write aggregate scorecard:
   - `~/.claude/state/bushel_7/aggregate_scorecard.json`
   - Total primitives audited / per-layer counts / per-shard counts
   - Status-field aggregates: how many primitives have all 6 surface statuses (pheromone + aa + cephas + code + mcp + hook + runtime) at "shipped/written/live/true"
   - Per-Candelabra-Core gap-list (highest priority — these block day-1 ship)
   - Per-Cooperative-Datacenter implementation roadmap with Founder-stated build-order baseline
   - Per-Patent-Reserve filing prioritization sorted by competitive_moat_strength desc
   - Aggregate completeness score (% of primitives with no gap-flags)
5. Write cost receipt:
   - `~/.claude/state/bushel_7/BUSHEL_7_COST_RECEIPT_BP021.json`
   - vendor_api_spend_usd / wall_clock_min / candlepower (target: 64 cP nested) / entries_written / per-Shadow cost breakdown
6. Run Codex Layer 8 binding:
   - `mcp__librarian__codex_create` — title: "Bushel 7 — 3-Layer Strategic Taxonomy + Coverage Audit (BP021)"
   - `mcp__librarian__codex_add_chapter` × 8 (one per Shadow shard) + 3 layer-manifest chapters + scorecard chapter + cost-receipt chapter (~12 chapters total)
   - `mcp__librarian__codex_review` (status flip to review)
   - `mcp__librarian__codex_bind` (HMAC-lock; assume Codex ID `LB-CODEX-0025` next-in-sequence post-`LB-CODEX-0024`)
7. Mirror substrate to repo:
   - `BISHOP_DROPZONE/04_KnightReports/BUSHEL_7_3_LAYER_TAXONOMY_COVERAGE_AUDIT_BP021.jsonl` — combined shards
   - `BISHOP_DROPZONE/04_KnightReports/BUSHEL_7_AGGREGATE_SCORECARD_BP021.json` — scorecard mirror

## Phase G — Verification Gates (no auto-retry; surface failures to Bishop)

- **G1 Coverage**: Combined entry count ≥75 (target ≥80). Shadow shards each ≥95% of their enumeration.
- **G2 12-field completeness**: Every audit entry has all 12 fields populated (no nulls in audit object).
- **G3 Layer assignment validity**: Every entry has `layer_assignment` ∈ {candelabra_core, cooperative_datacenter_dream, patent_reserve}.
- **G4 HMAC + Chronos + per-process salt**: All entries signed; no salt collisions.
- **G5 Evidence-grounded**: ≥90% of entries have at least 2 non-null evidence paths (eblet + code OR aa_formal + code OR similar).
- **G6 Rationale present**: All 3 rationale subfields populated per entry.
- **G7 Codex bound**: Codex `LB-CODEX-0025` HMAC-locked successfully.
- **G8 Repo mirror committed**: BISHOP_DROPZONE artifacts staged + commit + tag.

If any gate fails, surface to Bishop. Do NOT auto-retry. Substrate is preserved per BP020 canon — no work lost.

## Phase H — Commit + Tag + Stats-Capture

```bash
git add BISHOP_DROPZONE/04_KnightReports/BUSHEL_7_*BP021*
git add BISHOP_DROPZONE/01_KnightPrompts/PROMPT_KNIGHT_BUSHEL_7_3_LAYER_TAXONOMY_COVERAGE_AUDIT_BP021.md
git commit -m "BUSHEL-7-3-LAYER-TAXONOMY-COVERAGE-AUDIT LANDED -- N primitives audited / 3 layer manifests / aggregate scorecard / Codex LB-CODEX-0025 bound / 64 cP depth-3 nested all-Sonnet BP021"
git tag bushel-7-3-layer-taxonomy-coverage-audit-landed-bp021
```

Stats-Capture telemetry:

```jsonl
{
  "test_id": "bushel_7_3_layer_taxonomy_coverage_audit_bp021",
  "k_prompt": "PROMPT_KNIGHT_BUSHEL_7_3_LAYER_TAXONOMY_COVERAGE_AUDIT_BP021",
  "candlepower": 64,
  "architecture": "depth_3_nested_subagent_one_tab",
  "shadow_count": 8,
  "sub_subagent_count_per_shadow": 8,
  "sub_subagent_token_breakdown": [...],
  "vendor_api_spend_usd": <sum>,
  "wall_clock_min": ...,
  "primitives_audited": ...,
  "candelabra_core_count": ...,
  "cooperative_datacenter_dream_count": ...,
  "patent_reserve_count": ...,
  "completeness_score_pct": ...,
  "pass": true|false
}
```

Append to `~/.claude/state/bushel_7/bushel_7_stats.jsonl`.

## Phase I — Receipt to Founder

```
KNIGHT BUSHEL 7 — 3-LAYER STRATEGIC TAXONOMY + COVERAGE AUDIT — LANDED
Commit: <hash>
Tag: bushel-7-3-layer-taxonomy-coverage-audit-landed-bp021
Architecture: depth-3 nested (1 Knight × 8 Shadow × 8 sub-subagent = 64 cP all-Sonnet)
Codex: LB-CODEX-0025 HMAC <hmac>
Substrate aggregate: ~/.claude/state/bushel_7/aggregate_scorecard.json
Layer manifests:
  - ~/.claude/state/bushel_7/manifest_layer_1_candelabra_core.json (N primitives)
  - ~/.claude/state/bushel_7/manifest_layer_2_cooperative_datacenter_dream.json (N primitives)
  - ~/.claude/state/bushel_7/manifest_layer_3_patent_reserve.json (N primitives)
Shadow shards (8): ~/.claude/state/bushel_7/shard_*.jsonl
Repo mirror: BISHOP_DROPZONE/04_KnightReports/BUSHEL_7_*_BP021.*
Cost receipt: ~/.claude/state/bushel_7/BUSHEL_7_COST_RECEIPT_BP021.json
Wall clock: <min> | Vendor API spend: $<usd>
Aggregate completeness: <pct>%
Per-Candelabra-Core gap-list: <count> blockers (see scorecard.candelabra_gap_list)
Cooperative Datacenter build-order: Atreyu → Apiarist → Thirteenth Warrior (Founder-stated baseline; refined by audit)
Patent Reserve filing top-N: <list> (sorted by competitive_moat_strength desc)
```

---

## Discipline + Safety

- **DO** use TodoWrite to track Phases A→I
- **DO** use per-process salt for sub-subagent-generated IDs (BP018 KN-D2 fix)
- **DO** append-only JSONL semantics for concurrent writes
- **DO** ground every audit field in disk evidence; never invent status values
- **DO** flag genuine gaps honestly — gap-list is the highest-value Bushel 7 output
- **DON'T** skip Phase G gates to "ship faster"; substrate doesn't lie / status panels do
- **DON'T** apply rollup tags before Bishop verifies aggregates; per BP020 process-improvement
- **DON'T** propagate "publication gate" findings as Bishop-side constraints; per BP020 Fire Code canon, Founder Fire Code is universal gate
- **DON'T** attempt primitive-class re-classification or scope changes — those are Bishop+Founder decisions

## Recovery (if subagent crashes mid-fire)

Per BP020 substrate-as-immutable-backup canon: subagent state-loss is non-destructive. Append-only JSONL ledger preserves all completed work. Parent Shadow can re-spawn sub-subagent for remaining primitives only. Knight can complete Phase F from substrate ledgers. If Knight tab itself crashes, Bishop completes Phases F-I from substrate per BP020 canon (live-tested 2× in BP020 Bushel 1 + Bushel 2).

## Cleaner-than-found discipline

If during processing sub-subagents discover:
- Eblet path drift (Wrasse trigger collision / stale path)
- A&A formal misnumbered or missing
- Cephas page broken or orphaned
- Code path moved without manifest update
- MCP tool registered but not exported
- Hook wired but not firing

Log as cleaner-than-found findings in Phase I receipt. Do NOT attempt repairs unless trivial (one-line fixes); flag for Founder/Bishop instead.

---

## Productization angle (Bushel 7 self-shippable as LB Frame template)

Per Founder direct BP020 turn ~115: the audit Bushel itself is a shippable LB Frame product template — *"diagnostic / organization / prioritization Bushel"* that LB Frame end-users can run on THEIR own corpora. Solves real daily-user problem (audit-your-own-architecture).

Post-Bushel 7 LANDED, the LANDED K-prompt + sub-subagent template + 12-field audit schema become the shippable template for `librarian.lianabanyan.com` Federation-Member surface.

## Founder Fire Code reminder

This K-prompt fires only on Founder Fire Code (paste-and-GO in Cursor). Per BP020 Fire Code Universal Publication Gate canon, no publication / canonical-state-flip happens without Founder direct authorization. Knight executes the fire; Founder owns the fire decision.

---

*BP021 Bushel 7. Depth-3 nested. 64 cP all-Sonnet. Brick Wall.*
