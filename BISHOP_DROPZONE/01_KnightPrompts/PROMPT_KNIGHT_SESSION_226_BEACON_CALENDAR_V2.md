# KNIGHT SESSION 226 — BEACON + CALENDAR V2 DOMAIN MIGRATION

## Priority: HIGH (18/23 after this session)
## Estimated time: 60-90 minutes
## Domains: beacon, calendar

---

## CONTEXT

These are 2 of the final 4 substantive domains needing v2 migration. After this session + K227 (political + vehicle), we'll be at 21/23. Admin is cross-cutting (no pages), initiatives is empty — both may not need formal domain folders.

Follow the same v2 domain structure as K219-K224:
```
platform-v2/src/domains/{domain}/
├── pages/
├── components/
├── hooks/
├── lib/
├── routes.tsx
└── index.ts
```

---

## DOMAIN 1: BEACON (Wildfire Run System)

### V1 Assets
- **6 tables**: beacons, beacon_runs, beacon_run_progress, beacon_run_leaderboard, beacon_points, beacon_progress
- **1 edge function**: beacon-track
- **4 v1 pages**: WildfireRunsPage, BeaconRunCreator, BeaconExplainer, WildfireBeaconRun
- **2 migrations**: beacon_navigation_system, beacon_points_system

### What to Build

**Lib files:**
- `beaconTypes.ts` — BeaconRun, BeaconPoint, BeaconProgress, Leaderboard types
- `beaconRules.ts` — Constants (point values, run limits, leaderboard thresholds)

**Hooks:**
- `useBeaconRuns.ts` — CRUD for runs, progress tracking, leaderboard queries
- `useBeaconPoints.ts` — Points accumulation, progress tracking

**Components:**
- `BeaconRunCard.tsx` — Individual run display with progress bar
- `BeaconLeaderboard.tsx` — Ranked participants
- `BeaconPointsTracker.tsx` — Member's point accumulation display
- `BeaconRunCreatorForm.tsx` — Create new Wildfire runs

**Pages:**
- `WildfireRunsPage.tsx` — Main listing of active/past runs (AppShell)
- `BeaconRunDetailPage.tsx` — Single run with progress, leaderboard, participants (AppShell)
- `BeaconExplainerPage.tsx` — How beacons work (FocusShell, public)
- `BeaconRunCreatorPage.tsx` — Create new run (AppShell, Captain role)

**Routes:** 4 routes with FocusShell/AppShell wiring
**Barrel:** Full index.ts exports

---

## DOMAIN 2: CALENDAR (Event System)

### V1 Assets
- **2 tables**: calendar_events (17 cols), calendar_shares (6 cols)
- **3 edge functions**: calendar-sync-google, calendar-sync-commerce, calendar-create-event
- **1 v1 page**: Calendar
- **1 migration**: calendar.sql

### What to Build

**Lib files:**
- `calendarTypes.ts` — CalendarEvent, CalendarShare, EventCategory types
- `calendarRules.ts` — Constants (max events, share limits, sync intervals)

**Hooks:**
- `useCalendarEvents.ts` — CRUD events, filtering by date/category, share management
- `useCalendarSync.ts` — Google Calendar sync status, commerce event sync

**Components:**
- `CalendarGrid.tsx` — Month/week/day view grid
- `EventCard.tsx` — Individual event display
- `EventCreateForm.tsx` — Create/edit events
- `CalendarSharePanel.tsx` — Share calendar with other members
- `SyncStatusIndicator.tsx` — Google/commerce sync status

**Pages:**
- `CalendarPage.tsx` — Main calendar view with grid + event list (AppShell)
- `EventDetailPage.tsx` — Single event detail with RSVP/share (AppShell)

**Routes:** 2 routes, both AppShell (authenticated)
**Barrel:** Full index.ts exports

---

## VERIFICATION

After building both domains:
1. Run `npx tsc` — zero errors
2. Verify routes integrate with AppRouter
3. Rebuild Librarian index: `cd librarian-mcp && npx tsc && node dist/indexer/buildIndex.js`
4. Confirm v2-migration.json shows beacon + calendar as `migrated`

---

## DESIGN NOTES

- Beacon: No Pawn page design spec exists. Use the v1 pages as design reference. Maintain Wildfire Run branding.
- Calendar: No Pawn page design spec exists. Keep it clean — shadcn Calendar component, standard event CRUD.
- Both domains are AppShell-primary (member features, not public-facing conversion pages).
- BeaconExplainerPage is the only FocusShell page (public explainer).

---

*Generated by Bishop B061. FOR THE KEEP!*
