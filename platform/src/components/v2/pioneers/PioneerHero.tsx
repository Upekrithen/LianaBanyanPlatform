import { Card, CardContent } from "@/components/ui/card";

export function PioneerHero() {
  return (
    <Card data-xray-id="pioneers-hero">
      <CardContent className="py-4 text-sm text-muted-foreground">
        Early cohorts carried more uncertainty, and recognition reflects that. This is a living story still being written by every member.
      </CardContent>
    </Card>
  );
}
