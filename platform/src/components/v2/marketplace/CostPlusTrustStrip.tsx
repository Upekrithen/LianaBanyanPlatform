import { Card, CardContent } from "@/components/ui/card";

export function CostPlusTrustStrip() {
  return (
    <Card>
      <CardContent className="py-4 text-sm">
        Every listing follows the Cost + 20% floor. Creators keep 83.3%.
      </CardContent>
    </Card>
  );
}
