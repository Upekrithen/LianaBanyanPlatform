# .gitleaks.toml -- allowlist for confirmed false positives
# BP087 MAMBA-beta2 -- Sonnet 4.6 SEG audit
#
# Allowlisted fingerprints are pre-existing false positives verified by code review.

[allowlist]
  description = "Pre-existing false positive: ed25519 key generation variable name"
  commits = []
  paths = []
  regexes = []

  [[allowlist.findings]]
    # ed25519_keypair.ts line 55: `const keypair: Ed25519Keypair = { ... private_key_hex: ... }`
    # This is a runtime key generation helper -- no literal secret value present.
    fingerprint = "src/main/thorax/ed25519_keypair.ts:generic-api-key:55"
