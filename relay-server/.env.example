# WAN Relay — required environment variables (no values)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
PORT=8080
RELAY_ORIGIN=https://relay.lianabanyan.com

# Local dev only — bypass TLS check (never set in production)
# ALLOW_INSECURE=true
