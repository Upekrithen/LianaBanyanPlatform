import { ContentInfo } from "@peculiar/asn1-cms";
/**
 * ```asn1
 * TimeStampToken ::= ContentInfo
 *      -- contentType is id-signedData ([CMS])
 *      -- content is SignedData ([CMS])
 * ```
 */
export declare class TimeStampToken extends ContentInfo {
}
